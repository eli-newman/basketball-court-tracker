supervision>=0.19.0
opencv-python-headless>=4.8.0
numpy>=1.24.0
requests>=2.28.0
scikit-learn>=1.3.0

# Optional — only needed when --backend local is used:
#   pip install inference        # CPU
#   pip install inference-gpu    # CUDA (Colab T4)
# Note: inference requires Python 3.10–3.12 (not 3.13).
